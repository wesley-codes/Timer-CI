apiVersion: v1
kind: Service
metadata:
  name: {{ include "timerapp.fullname" . }}
  labels:
    {{- include "timerapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "timerapp.selectorLabels" . | nindent 4 }}
