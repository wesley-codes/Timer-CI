apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "timerapp.fullname" . }}-test-connection"
  labels:
    {{- include "timerapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "timerapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
